# Historical prototype reference

This directory preserves the original single-file LyricLingo marketing prototype and its asset-generation references for migration comparison only. It is not part of the Next.js runtime.

The binary media moved to `public/` so the production application can reuse the original files without duplication.
